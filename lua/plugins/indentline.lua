require("ibl").setup(
    {
        indent = {
            char = "▏",
            tab_char = { "a", "b", "c" }
        },
        scope = {
            show_start=false,
            show_end=false,
        }

    }
)
